{
  "name": "server",
  "private": true,
  "version": "1.0.0",
  "description": "Azure Functions app for Quality HealthCare booking",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "prestart": "npm run build",
    "start": "func start"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "@azure/functions": "^4.6.0",
    "mssql": "^11.0.1"
  },
  "devDependencies": {
    "@types/mssql": "^9.1.7",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
